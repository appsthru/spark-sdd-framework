# spark-sdd-framework
Minimalist framework to frame your projects and avoid bloat.

## GitHub Pages

- The one-page site lives in `/site`.
- The publication workflow is in `/.github/workflows/deploy-doc-site.yml`.
- Push the `doc` branch to trigger deployment to GitHub Pages.
